# from flask import Flask
# import views

# app = Flask(__name__)

# app.add_url_rule('/<name>/<int:marks>', 'index', views.index)
# app.add_url_rule('/forloop', 'index_for', views.index_for)

# if __name__ == '__main__':
#     app.run(debug=True)